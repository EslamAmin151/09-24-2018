class Bank_account:
    def __init_(self,first_name,last_name,middle_name, account_type,balance,status)
    self.first_name = first_name
    self.last_name = last_name
    self.middle_name = middle_name
    self.account_type = account_type
    self.balance = balance
    self.status = status
    self_total = 0

    def open_bank_account(self):
    start = ["press O to open bank account"]
    i
    self.open_bank_account





   def transfer(self, amount):
    self.total = self.total - amount
    ID.total = ID.total + amount
    return ID.balance(


    def withdraw(self, amount):
        if amount > self.balance:
            raise ValueError("insufficient funds")
        print self.balance -= amount

    def charge(self,amount):
        if (self.balance <= 0):
            print "There will be 35$ charge"
        else:


Bank_account = ["Eslam","Amin","A","Checking",1000,"open"]
